// event-bus.js   //maybe to delete, to investigate(sends error status from login view to app, but this status doesn't demonstrate in app view)
import Vue from 'vue';
const EventBus = new Vue();
export default EventBus;